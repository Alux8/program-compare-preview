import React from "react";

type DesktopCompareTableProps = {
  children: React.ReactNode;
};

export default function DesktopCompareTable({ children }: DesktopCompareTableProps) {
  return <>{children}</>;
}
